import styled from "styled-components";

export const Button = styled.button`
  all: unset;
  cursor: pointer;
  margin: 0.5em;
  margin-top: 3em;
  padding: 0.5em;
  font-size: 1.25rem;

  &:hover {
    color: rgb(182, 143, 255);
  }
`;
